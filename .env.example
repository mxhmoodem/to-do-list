# MongoDB Connection
# For local MongoDB: mongodb://localhost:27017/todolist
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/todolist
MONGODB_URI=mongodb://localhost:27017/todolist

# JWT Secret Key
# Generate a random string for production (e.g., use a password generator)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port (Railway will set this automatically)
PORT=3000

# Node Environment
NODE_ENV=development
